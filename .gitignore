node_modules/
__pycache__/

# Firebase Admin SDK service-account keys (see scripts/admin-data-cleanup.js) -- these are real,
# standing admin credentials for the whole project and must never be committed.
*serviceAccount*.json
*service-account*.json
